import 'package:flutter_test/flutter_test.dart';

import '../lib/cloudinary_widget.dart';

void main() {
  test('adds one to input values', () {
    //  ignore: unused_local_variable
    final cloudinarywidet = CloundinaryWidget(
        cloudName: "null", uploadPreset: "null", onChanged: (String string) {});
  });
}
